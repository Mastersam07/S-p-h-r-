import 'package:flutter/material.dart';

class ViewAThree extends StatelessWidget {
  const ViewAThree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View A Three'),
      ),
    );
  }
}
