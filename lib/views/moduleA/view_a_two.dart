import 'package:flutter/material.dart';

class ViewATwo extends StatelessWidget {
  const ViewATwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View A Two'),
      ),
    );
  }
}
