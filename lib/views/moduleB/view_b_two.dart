import 'package:flutter/material.dart';

class ViewBTwo extends StatelessWidget {
  const ViewBTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View B Two'),
      ),
    );
  }
}
