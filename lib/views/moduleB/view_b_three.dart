import 'package:flutter/material.dart';

class ViewBThree extends StatelessWidget {
  const ViewBThree({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View B Three'),
      ),
    );
  }
}
